import { env } from 'cloudflare:workers';
import { NextResponse } from 'next/server';
import { getChatGPTUser, isApprovedEmail } from '../../chatgpt-auth';
async function authorised() { const user = await getChatGPTUser(); return user && isApprovedEmail(user.email) ? user : null; }
export async function POST(req: Request) { const user = await authorised(); if (!user) return NextResponse.json({ error: 'Access denied' }, { status: 403 }); const form = await req.formData(); const file = form.get('file'); const recordId = Number(form.get('recordId')); const fieldName = String(form.get('fieldName') || 'document'); if (!(file instanceof File) || !recordId || file.size > 10_000_000) return NextResponse.json({ error: 'A valid file up to 10 MB is required' }, { status: 400 }); const allowed = ['application/pdf','image/jpeg','image/png']; if (!allowed.includes(file.type)) return NextResponse.json({ error: 'Only PDF, JPG and PNG files are accepted' }, { status: 400 }); const key = `${user.userId}/records/${recordId}/${crypto.randomUUID()}-${file.name.replace(/[^a-zA-Z0-9._-]/g, '_')}`; await env.BUCKET.put(key, file.stream(), { httpMetadata: { contentType: file.type } }); const result = await env.DB.prepare('INSERT INTO compliance_documents(owner_id,record_id,field_name,file_name,content_type,object_key,uploaded_at) VALUES(?,?,?,?,?,?,?)').bind(user.userId, recordId, fieldName, file.name, file.type, key, new Date().toISOString()).run(); return NextResponse.json({ id: result.meta.last_row_id, fileName: file.name }, { status: 201 }); }
export async function GET(req: Request) { const user = await authorised(); if (!user) return NextResponse.json({ error: 'Access denied' }, { status: 403 }); const id = Number(new URL(req.url).searchParams.get('id')); const row = await env.DB.prepare('SELECT * FROM compliance_documents WHERE id=? AND owner_id=?').bind(id, user.userId).first<{ object_key: string; file_name: string; content_type: string }>(); if (!row) return NextResponse.json({ error: 'Document not found' }, { status: 404 }); const object = await env.BUCKET.get(row.object_key); if (!object) return NextResponse.json({ error: 'Document not found' }, { status: 404 }); return new NextResponse(object.body, { headers: { 'content-type': row.content_type, 'content-disposition': `attachment; filename="${row.file_name.replaceAll('"','')}"`, 'cache-control': 'private, no-store' } }); }

import { env } from 'cloudflare:workers';
import { NextResponse } from 'next/server';
import { getChatGPTUser, isApprovedEmail } from '../../chatgpt-auth';
export async function POST(req: Request) { const user = await getChatGPTUser(); if (!user || !isApprovedEmail(user.email)) return NextResponse.json({ error: 'Access denied' }, { status: 403 }); const body = await req.json() as { bookingId: number; recipient: string; message: string }; const result = await env.DB.prepare('INSERT INTO message_logs(owner_id,booking_id,channel,recipient,message,status,actor_email,created_at) VALUES(?,?,?,?,?,?,?,?)').bind(user.userId, body.bookingId, 'SMS', body.recipient, body.message, 'PREPARED', user.email, new Date().toISOString()).run(); return NextResponse.json({ id: result.meta.last_row_id, status: 'PREPARED' }, { status: 201 }); }

import { integer, real, sqliteTable, text } from 'drizzle-orm/sqlite-core';
export const bookings = sqliteTable('bookings', { id: integer('id').primaryKey({autoIncrement:true}), ownerId:text('owner_id').notNull(), passengerName:text('passenger_name').notNull(), phone:text('phone').notNull().default(''), pickup:text('pickup').notNull(), dropoff:text('dropoff').notNull(), pickupAt:text('pickup_at').notNull(), operator:text('operator').notNull().default('APX RIDE'), passengers:integer('passengers').notNull().default(1), largeBags:integer('large_bags').notNull().default(0), smallBags:integer('small_bags').notNull().default(0), fleetTier:text('fleet_tier').notNull().default('Saloon'), distance:real('distance').notNull().default(0), fare:real('fare').notNull().default(0), status:text('status').notNull().default('upcoming'), notes:text('notes').notNull().default(''), createdAt:text('created_at').notNull(), updatedAt:text('updated_at').notNull() });
export const settings = sqliteTable('settings', { ownerId:text('owner_id').primaryKey(), fareModel:text('fare_model').notNull().default('A'), fuelPer100:real('fuel_per_100').notNull().default(50), includeFuel:integer('include_fuel',{mode:'boolean'}).notNull().default(true), reviewUrl:text('review_url').notNull().default(''), updatedAt:text('updated_at').notNull() });
